---
title: "Of mice and menus: Loopllama and the tragedy of UX"

excerpt: >
  The mouse was never the right tool for routine operations — a point its
  inventor understood and the industry forgot. Grammar-based keyboard systems
  are a demonstrably better alternative. The tragedy of UX is that the
  ecosystem made them the hard path.

tags:
  - loopllama
  - software
  - ux

published: false
---

<!--

## S1: The trouble with mice
## S2: The command-line tradition and its constraints
## S3: Raskin's case against modes
## S4: The Canon Cat: when theory meets practice
## S5: Modes of inevitability
## S6: Modes all the way down
## S7: The ecosystem problem

Modifiers available for application-level bindings on each OS:

    Role as a modifier | Mac    | Windows | Linux
    ----------------------------------------------
    Primary            | Cmd    | Ctrl    | Ctrl
    Secondary          | Ctrl   | Alt     | Alt
    Tertiary           | Option | -       | -
    Augmenter          | Shift  | Shift   | Shift
    Mostly OS-reserved | -      | Win     | Super

-->

All software faces a basic question: how does the user tell it what to do?
Since the 1980s the dominant model has been drearily consistent: reach for the
mouse, navigate into an application's menus, and click the needed operation.
Ordinary navigational tasks are similar, minus the menus: reach, point,
scroll, click. Keyboard shortcuts for some of those operations do exist.
Nearly all users learn a handful — copy, paste, print, arrow keys — and stop
there. Some users invest real effort to learn the bindings of applications
they use heavily, but even the office expert in Excel eventually hits a
ceiling: the brain will hold only so many arbitrary CTRL-ALT combinations.

I recently completed [LoopLlama][llv2], a browser-based tool for close study
of YouTube videos. The app has menus and familiar mouse-oriented controls such
as buttons, dropdowns, and toggles. But at its core, LoopLlama has a
keyboard-first design: my priority was to be able to do everything efficiently
with simple key presses while holding a guitar and wearing a thumb pick,
without tedious pointing and clicking. Building it sharpened my convictions
about keyboard design and produced a working example of a better UX paradigm —
one with strong historical precedents but that, tragically, was ignored as our
computing culture solidified in the late 20th century.

## S1: The trouble with mice

The mouse is an instrument tuned for actions that are inherently spatial:
pointing, dragging, positioning, drawing — tasks with a direct mapping between
hand movement and cursor position.

Command input is not that. When a user reaches for the mouse to invoke a menu
operation or perform a routine navigation, nothing about that operation
requires pointing. The action is discrete: the user wants to save a file, undo
a change, or toggle a setting. The mouse arrives at the target by the same
mechanism it uses to draw a curve — slow physical travel, target acquisition,
and click — but the curve-drawing precision is wasted. You do not need a
pointing instrument to say "save." Like a caveman, you point for routine
operations only when you lack the words.

The costs are individually tiny but undeniably real in aggregate. Across a day
of routine work, the mouse traversals compound into a hefty ergonomic load.
Perhaps less obviously but more importantlay, mouse operations are not readily
scripted, chained, or automated. The keyboard, by contrast, is already where
the hands are, and its input is discrete, precise, and directly amenable to
composition in larger, automatable opererations.

The case for the keyboard as the natural command instrument is not subtle, and
the problem was recognized early in computing history. Two examples will suffice:

- [Douglas Engelbart][douglas_engelbart] led the Augmentation Research Center
  Lab in SRI International during the 1960s. In 1968, Engelbart and other ARC
  staffers presented their [oN-Line System][nls] (NLS), which included many of the
  elements of modern, interactive computing, including the computer mouse. The
  presentation was so striking to subsequent observers that be become known as
  [The Mother of All Demos][mother_demos]. Working toward the larger goal of
  making knowledge work more powerful, Engelbart and his team understood the
  division of labor sketched above. NLS paired the mouse with the keyboard:
  the mouse for spatial tasks, the keyboard for text entry and commands.[^1]
  Only the pointing half of that vision survived contact with the marketplace.
  The resulting arrangement — mouse for everything, including commands — was
  not what Engelbart had in mind.

- Later in the 1980s and 1990s, human-computer interface expert [Jef
  Raskin][jef_raskin] made the case in more rigorous terms. His work drew on
  two tools from the empirical literature. [Fitts's Law][fitts_law] provides a
  predictive model for target-acquisition time as a function of distance and
  target size. Applied to command input, it quantifies the cost of
  keyboard-to-mouse traversal on every command. The [Keystroke-Level
  Model][keystroke_model], a framework for predicting task completion time
  from its physical components, confirms the pattern: mouse operations are
  consistently and measurably slower than keyboard equivalents for discrete
  commands.

## S2: The command-line tradition and its constraints

Although the consumer market would become dominated by mouse-and-menus, an
older tradition — Unix and the command-line world — had built a culture
around keyboard primacy: application bindings, keyboard shortcuts, editors
with layered command vocabularies. Since command-line computing became
powerful before the mouse existed as a widely available device, the mouse
could, at best, augment command-line computing at the margins, but it would
never be central.

The applications running in that computing tradition faced an unavoidable
constraint: large application have large command vocabularies, but the
keyboard offers limited real estate. A typical modern keyboard has 50 keys
producing regular characters, and only 26 of them (the letters) have obvious
mnemonic potential.[^2] LoopLlama, for example, is a rich application for its
purpose, but it is tiny compared to the software that computer users spend the
most time with: word processors, spreadsheet applications, web browsers, and
many others. LoopLlama has roughly 100 key bindings to cover its operations
— already twice the keyboard's binding capacity.

The dominant answer to that constraint was keyboard expansion, along two
pathways. One was additive: function keys, a dedicated row of extra keys above
the main keyboard — provided twelve new slots available for direct binding.

The second was more powerful because it was, or at least could be,
multiplicative: modifier keys. A keyboard attached to a computer has an
intuitive default: every key emits a character. To invoke an operation rather
than emit a character, you need the hardware event (the key press) to send a
signal other than the key's default character. Typewriters already supplied a
model in the `Shift` key, which produces a different letter or a punctuation
mark. CTRL was the computing extension of that idea. In early keyboards,
holding CTRL while pressing a letter resulted in a different ASCII code than
the unmodified key press. Because of constraints of the ASCII system and the
hardware commonly available, only 32 ASCII codes were reserved for CTRL
(letters plus 6 punctuation marks). So CTRL, strictly speaking, was another
additive expansion. Later, hardware introduced in the consumer market spread
into command-line environments (Alt from the IBM PC, Cmd and Option from the
Mac).

By the end of the 1990s, a keyboard-oriented user would have not only more
keys (12 function keys, arrows, and perhaps even expansion areas for a number
keypad or various navigational keys) but also several modifiers. Those
additions faced some hardware and encoding limits too, as well as various key
reservations by the operating system. But one could fairly say the net effect
of those keyboard expansion pathways was multiplicative. Instead of having
only the 32 ASCII Ctrl code available for binding to commands, applications
running in those environments would have hundreds of slots available for
binding — roughly 800 based on my back-of-the-envelope calculations.

Although the expansion of key binding real estate was impressive in
quantitative terms, most of that terrain turned out to useless in practice,
because humans cannot remember the bindings. The letter component of a binding
can carry genuine meaning: `Cmd-p` for print, `Cmd-s` for save, `Cmd-c` for
copy. But the modifiers themselves are abstractions. Any given operation might
use `Cmd`, `Cmd-Shift`, `Cmd-Option`, or something more elaborate. Under
favorable conditions, a well-designed application could group bindings
thematically under different modifer combinations to mitigate the mnemonic
problems. In practice, the logic behind the binding schemes of many major
applications is somewhere between opaque and non-existent.

The other problem with the modifier strategy is physical. Although Shift sits
at the natural extension of the pinky's reach, the primary modifiers for
issuing commands are ergonomic disasters, involving either awkward stretches
to reach a single modifer or completely shifting the hand out of typing
position to press more than one.

In the consumer market, an unfortunate third pathway emerged as a response to
the keyboard real estate challenge: for many operations, there would be no key
binding at all. Most users learned a handful of keyboard shortcuts and stopped
there. The rest of their command vocabulary lived behind menus and long days
of pointing and clicking.

One cultural response to those constraints were calls to embrace expertise.
Such reactions sometimes took the form of specific individuals investing
sufficient effort to learn an application's incoherent list of modifier-based
bindings, drilling it into muscle memory over years. Another reaction took the
form of a wholesale critique of not so much of the mouse but of the GUI
itself, perhaps most famously in Neil Stephenson's [essay][stephenson_essay],
"In the Beginning... Was the Command Line," which constrasted the power of
command-line computing (his metaphor was Linux as a freely available tank)
against the limitations and frustrations of GUI computing (Windows as a
station wagon and Mac as a sports car, both prone to breakdowns). Stephenson
understood why so many users opted for the initial ease and reassurance of
purchasing from friendly dealer, but he upheld the virtues of the minority
that would invest the time to learn how to drive and maintain a tank.

Of course, most users will never invest that heavily in most of the software
they use, because their usage will not be substantial enough to justify the
cost. On the other hand, most people have at least one application they use
often enough to warrant such investment: the person who spends a significant
chunk of every workday in Word, Excel, or Outlook; the retiree who manages
photos in Lightroom every weekend; the teacher who assembles lecture slides in
PowerPoint. Additional expertise would pay. But if the route to expertise goes
through a thicket of modifier-based key bindings, few will make the journey.

## S3: Raskin's case against modes

Jef Raskin, one of the human-computer interface experts mentioned above, had
long been unhappy with both computing paradigms on offer. On one pole was the
expert-oriented computing tradition of Unix and the command-line. Although
powerful, this user interface provided poor visual feedback, was recall heavy,
and demanded exact syntax with no tolerance for approximation. In any case,
since it had already lost any claim on the mainstream, it was not his primary
target. Meanwhile, the GUI systems from Windows and Apple had become almost as
opaque as the command-line systems they replaced, with deep menu hierarchies
that required users to know where to look when trying to perform a task — a
mnemonic burden in a new form.

In 2000 Raskin published [The Humane Interface][humane_interface], which
consolidated and elaborated on the critiques of the prevailding computer
interfaces that he had been developing since the 1980s. The book's guiding
claims were that interfaces should prioritize alignment with human cognition
rather than computer internals and that design should aim to increase
efficiency for all users, not just experts willing to commit time to mastering
difficult systems. At least in principle, both ideas implied an empirical
orientation to observe real behavior with any proposed interface.

Both cognitive science and everyday experience show that practiced actions
become habitual, automatic: driving, typing, using a TV remote, riding a
bicyle. A central idea in Raskin's work is that good interfaces should aim to
provide a basis for automaticity to form — for users to stop consciously
deciding and simply to act. By constrast, interfaces that change from version
to version; that have different behaviors in different contexts, forcing user
to track state mentally; that have a lack of feedback or large delays between
action and response; that have high error costs (for example, no undo),
forcing users into hyper-diligence — such traits force users to act
consciously rather than automatically.

Raskin identified modes as the most systematic of these failures. A mode is a
state of the interface in which the same user action produces different
results. The computing environment of the 1980s and 1990s supplied vivid
examples: the Caps Lock key, inherited from the typewriter, which silently
shifted every subsequent keystroke to uppercase; word processors that toggled
between inserting and overwriting text; image editors and presentation tools
that required the user to track which drawing tool was active; as well as one
of the most prominent text editors in the command-line world, vi, which was
built on top of a modal foundation. In each case, the same action — a
key press or a mouse click — produced different results depending on
application state the user had to remember.

In such interfaces, mode errors can occur any time the user becomes too
preoccupied or rushed with their work and forgets which mode is active before
typing or clicking. Such errors were bothersome for novice users, of course,
but Raskin's deeper critque emphasized how they worked to undermine the
automaticity upon which expertise rested. The same automatic firing that makes
experts efficient makes them particularly vulnerable to mode errors: if you
wrongly think you are in insert-mode in vi, the more letters you type, the
more likely you are to do some damage.

Raskin's prescription followed from the diagnosis: modes should be eliminated
whenever possible. Where they could not be, his preferred alternative was the
quasimode — a held key, active only while pressed. Unlike a persistent mode, a
quasimode cannot be easily forgotten: the interface's mode switch becomes
obvious, tactile.

## S4: The Canon Cat: when theory meets practice

Although Raskin had played a leading role in an early phase of the Macintosh
project, he was disappointed by the result — too mouse-heavy, too dependent on
menus, too little oriented toward the cognitive framework he was developing. In
1987 he led the design of an alternative: the Canon Cat, a word-processing
computer that provided an early working example of the interface principles he
would later codify in The Humane Interface.

    Links to add:

      # Leap technlogy promo.
      https://www.youtube.com/watch?v=o_TlE_U_X3c

      # Wiki
      https://en.wikipedia.org/wiki/Canon_Cat

The Cat's design consciously rejected two of the three prevailing strategies
for solving the keyboard real estate problem. The surrender strategy — putting
lower-priority operations behind menus and requiring the user to point and
click for anything without a binding — was unacceptable on the terms Raskin
had already established. The third strategy — embracing modes more fully —
addresses real estate by allowing the same keys to serve different functions
depending on the interface's current state. That too was off the table for
Raskin; modes were the primary target of his critique. That left keyboard
expansion, specifically the modifier approach, as the only viable path. The
Cat innovated on that approach in two ways.

    Links to add:

      # Cat keyboard image
      https://vintagecomputer.ca/wp-content/uploads/2016/04/Canon-Cat-keyboard.jpg

The first was a pair of USE FRONT keys, positioned on the left and right sides
of the spacebar — replacing the usual modifier keys in that position. The USE
FRONT keys activated a quasimode: hold the key and the keyboard's behavior
changed, the same principle as Shift or CTRL. The payload was printed on the
front edges of the keys themselves — Copy, Calc, Print, Bold, Indent, Spell
Checker, and so on. To invoke an operation, the user pressed and held USE
FRONT with one thumb and pressed the labeled key with a finger. The operation
catalog was visible on the hardware, eliminating recall as a prerequisite for
access.

The second innovation was a pair of LEAP keys, positioned below the spacebar
for thumb operation — a significant ergnomic improvement relative to the
placement of the usual modifier keys. LEAP was the Cat's primary navigation
primitive. Holding LEAP while typing a literal string moved the cursor
dynamically through the document as each character was entered — an
incremental search. The same mechanism supported text selection: position the
cursor at one endpoint, then LEAP to the other; pressing both LEAP keys
simultaneously highlighted the text between them. Text movement followed
naturally: select a block, LEAP to the destination, and the selection
relocated. Like USE FRONT, the LEAP keys were quasimodal, active only while
held, so their effect on the keyboard's behavior was always explicit and
tactile.

The Cat sold poorly and was discontinued after roughly a year. That failure
could be attributed to business strategy or the vagaries of a fluid computing
market in 1987 — plausible enough explanations, and probably partial ones. My
retrospective judgment, though, is that the Cat was built on a deeply flawed
vision, and that the flaws were systematic rather than incidental. They
cluster around three aspects of the design.

The first flaw is that the Cat does, in fact, adopt the surrender strategy —
just in a different form than the mouse-and-menu world did. The GUI market
surrendered by exiling lower-priority operations to menus, forcing users to
reach for the mouse whenever they needed something without a binding. The Cat
avoided that move, but only by drastically restricting what the device was
expected to do. At its core, it was a word-processing appliance, not a
general-purpose computer. It had computing at the margins — a calculator
accessible via the CALC key, and a lower-level toolchain for Forth and
assembly — but it had no support for third-party software. The operation
catalog could fit on the front edges of the keys precisely because the domain
was narrow enough for that to be possible. A general-purpose computer has no
such catalog to print.

The second flaw is that LEAP, as the Cat's sole navigation primitive, failed
both ends of the user spectrum, leaving it in an interface sour spot.

For beginners, the Cat was a RTFM device from the start. It had no arrow keys.
The LEAP keys could move the cursor one character at a time — the manual calls
this "creeping" — but the manual admonishes users not to rely on creeping as a
crutch. Once a user did learn to LEAP, the physical tax was permanent: every
navigation required holding LEAP while typing a search string. That is the
inescapable cost of the quasimode approach — the same cost paid by every
modifier scheme. For less frequent operations, the key sequences required
could feel like a convoluted finger dance sometimes documented in the user
manual in ways that felt like a brain puzzler.

Intermediate and expert users hit low ceiling. Leaping was literal text
search: no wildcards, patterns, regex, or text objects. GUI word processors
had long offered at least wildcards and navigational support for words and
paragraphs. Command-line editors went much farther on both fronts. The best
the user manual could offer were approximate literal-search workarounds for
some text objects: space for word boundary, period for a sentence, two returns
for a paragraph.

The third flaw cuts deepest, because it is a theory violation, not just a
limit. The Cat's cursor had two states — wide and narrow — that controlled the
direction of text erasure and whether typing would insert or replace existing
text. By Raskin's own definition, this is a mode.

More striking still: the same behavior is found in vi, the command-line text
editor proudly standing on the opposite side of the modes debate. Vi users
navigate among normal mode, insert mode, and replace mode — distinctions
conveyed via cursor shape, just like the Cat. The most determined opponent of
modes in computing built them into his signature product.

## S5: Modes of inevitability

The Cat's cursor mode is a non-trivial violation of Raskin's own theory and a
clue — a suggestion that wisdom lies in the recognition that modes are
unavoidable.

<span class="phead">Applications required</span>. The logical case is direct
and begins with the inevitability of applications. To modern computer users,
that is a puzzling place to start — a claim so obvious as to be hardly worth
making. But Raskin's writing and the Cat's advertising material sometimes
conveyed the idea that computer users should not have to bother with pesky
details like applications, desktops, directories, and even files. At the same
time, however, a [promotional video][cat_promo] for the Cat explicitly
mentions spreadsheets and database applications running on the device. In some
sense, the admission was compelled because, by 1987, such applications were
well on their way to becoming standard in office computing. The video glosses
over the apparent contradiction — was the Cat an appliance optimized for
editing text or a general-purpose computer? — by showing only toy examples:
small spreadsheet tables or data-editing forms embedded within a larger text
document. Anyone who has spent serious time in a spreadsheet application knows
how fanciful that vision is. A spreadsheet is not a word processor, and a
database application is not a spreadsheet. Each involves different data,
different tasks, and thus different interfaces. Cramming Excel into a text
editor will not cut it.

<span class="phead">Applications are modes</span>. Each application's distinct
interface is, by definition, a mode: a state in which the same keyboard and
mouse inputs produce different results. To grant that different applications
require their own interfaces — and there is no plausible argument against this
— is to grant that some modes are unavoidable.

<span class="phead">Rinse and repeat: modes within applications</span>. The
same reasoning applies at the next level down. Nearly all non-trivial
applications need to support multiple kinds of work. Mapping applications have
searching and exploring, but also direct navigation to a specific destination.
Presentation software has the task of creating slides as well as presenting
them. More directly relevant for our purposes, many applications — notably,
word processors, text editors, and spreadsheets — need to support at least two
types of work: directly entering literal text versus issuing various commands
for editing and navigation. In order to be effective and efficient, the
interfaces for the different tasks will necessarily be different: the bigger
the task difference, the bigger the interface difference. Modes necessarily
enter the scene.

<span class="phead">Refutation by counterexample: QED</span>. If computing
operates with at least two irreducible layers of modes — applications and then
major tasks interfaces within them — a blanket prohibition cannot stand. In
fairness, Raskin's views were not that rigid, but he did make strong claims
about the harmfulness of modes and his interest in eliminating them to the
extent possible. At this point, however, a strong anti-modes posture has lost
its confident footing: the debate is no longer whether modes are harmful or
should exist; the terrain necessarily shifts to sharper questions.

The relevent questions deal with the characteristics that make modes
beneficial, cognitively manageable, and worth having. Four seem most relevant.

  - Visibility. A mode the user cannot readily see is on the user will forget,
    leading to errors. A clear indicator keeps the current mode in peripheral
    awareness without demanding attention.

  - Task clarity. The mode should correspond to a phase of work the user
    recognizes as meaningfully distinct. A mode organized around a genuine
    task distinction gives users a natural mental model: they know which mode
    they should be in because they know what they are doing.

  - Switching cost-benefit. The transition into and out of the mode should be
    deliberate and low-cost — easy enough that users do not avoid it, bounded
    enough that accidental activation is unlikely. In addition, the key or
    gesture used to enter the mode occupies real estate that could serve other
    purposes, and that expenditure should be proportionate to the mode's value
    in supporting useful work.

  - Risk. When the user acts in the wrong mode, what happens? Some errors are
    trivially reversible; others can destroy work. The severity of potential
    mode errors is part of the cost-benefit ledger.

Two examples show how these criteria play out in practice:

  - Caps Lock fails on most of them. Its indicator — a small LED on most
    keyboards — is easy to overlook, particularly on laptops. Accidental
    activation is common: the key occupies prime real estate at the left edge
    of the home row, where an errant pinky routinely lands. And the task it
    serves — sustained all-caps typing — is infrequent for most users, with
    cheap workarounds available in any modern editor. Valable keyboard real
    estate, easy accidental activation, low-frequency task: the cost-benefit
    is badly skewed.

  - Vi's insert and normal modes tell the other story. The task distinction
    is genuine — one mode for entering literal text, one for navigation and
    editing commands — and users learn it readily because it tracks something
    real about the work. Switching cost is low: a single keypress in each
    direction. The risk from mode errors is real but substantially mitigated
    by vi's strong undo system. The main shortcoming is visibility: a
    cursor-shape change, the same mechanism the Cat used — a real but subtle
    mechanism that is arguably not as prominent as it could be, especially on
    modern computer displays. Nonetheless, on balance, these modes earn their
    place.

Raskin's core concern about modes was automaticity: that users would have to
maintain conscious awareness of the current mode state, and that this overhead
would prevent frequent actions from becoming practiced habits. The concern is
real for poorly designed modes, but it gets the relationship backward for
well-designed ones. A mode organized around a genuine task distinction does
not burden the user's cognitive apparatus — it becomes part of it. In vi,
normal mode is for moving around and manipulating text; insert mode is for
typing. Neither interferes with the other because the system's organization is
sensible, intuitive. The modes become an organizing hook — similar to the
conceptual order supplied at a higher level by applications — and thus become
a stable and meaningful base upon which automaticity can form.

## S6: Modes all the way down

Accepting that modes are inevitable, and that well-designed ones support
rather than undermine automaticity, opens a further opportunity: modes can
become a direct solution to the keyboard real estate problem that drove
computing toward modifiers and menus in the first place.

That problem was a mismatch between command vocabulary size and available
binding slots, producing the strategies already described: more modifier
combinations, function keys, surrender to menus. All of them shared a hidden
assumption: that the keyboard's default job is to emit characters, so bindings
must be carved out against that baseline.

A dedicated typing mode dissolves the assumption. Once the application has a
specific mode for literal text entry, every other mode is free: in command
mode, no key is obligated to emit its character. The letters, digits, and
punctuation marks become binding real estate — no modifier required, no
awkward reaches, no shifting hands out of typing position. Every key,
especially the letters, have the potential to carry meaning directly (`d` for
delete, `w` for word). None of the keys are pure abstractions the way the
modifiers are. A typing mode is not just a concession to inevitability; it is
an architectural move that restructures the real estate problem entirely.

That key and mnemonic expansion is the first step. The second adds structure
to the binding scheme — a move that not only expands the available command
space but makes it more meaningful and memorable. The normal-mode bindings of
vi are illustrative. That command space is not a flat list of bindings to be
memorized; it is built from a small vocabulary of atoms governed by a grammar,
and the grammar generates operations combinatorially.

The atoms come in three roles. Verbs express operations: `d` for delete, `y`
for yank (copy), `c` for change. Nouns name text objects or navigation
targets: `w` for word, `ap` for a paragraph including its surrounding blank
lines, `ip` for the paragraph's inner text. Quantifiers are numeric prefixes
that scale the verb, the noun, or both. Users learn those atoms individually,
and the grammar then makes their combinations largely derivable. `dw` deletes
a word; `d5w` deletes five; `yap` yanks the current paragraph. Some verbs can
act as stand-alone imperatives — `i` to enter insert mode, `p` to paste — with
no noun required.

The cognitive arithmetic is favorable. [Rest of paragraph isn't working.]
Learning the common atoms requires memorizing a modest vocabulary. But the
grammar makes those atoms productive beyond what rote memorization could
achieve: a user who knows ten verbs and ten nouns does not have a hundred
discrete bindings to memorize; they have a hundred operations they can derive.
The structure does the memorization work.

    - Then they can deploy those atoms according to the grammar:

    - To perform the most common types of editing work requires memorizing a
      fairly modest number of these primitives. After that's done, the user
      can combine them in many ways.
      - A small vocabulary of components generates a very large command space.
      - An extemely favorable ratio between operational benefit and
        memorization cost.
      - The grammatical structure and its rules provide the foundation upon
        which effective memorization can occur.

Grammars and multi-key bindings:

  - The structuring quality of grammatical binding schemes points the way
    toward creating even more effective systems:
  - By adding more structure.
  - Specifically, additional modes via the mechanism of multi-key bindings.
  - This technique not only leverages the cognitive benefits of meaningful
    structure.
  - It also has a massive impact on the keyboard real estate problem:

    - Consider two approaches for how one could organize the commands in vi's
      normal mode, using a simplified model of the keyboard just to key to
      example manageable:

      - Modifer strategy:
        - Starting base: 26 letters for bindings.
        - Mod: Shift.
        - Result: 26 * 2 = 52 bindings

      - Multi-key strategy:
        - Same base.
        - No mods.
        - All bindings will be 2-character.
        - Result: 676 bindings

    - Compared to the modifier approach, a multi-key system:
      - Has a real cost:
        - Bindings require 2 chars.
        - But so does every modified binding.
      - Has better ergonomics:
        - No awkward reaches.
        - No double key presses at all.
        - Hands never need to leave typing position.
      - Massively expands key real estate and, just important, mnemonic space:
        - Modifier strategy:
          - Each meaningful key (letter) can be bound to only 2 ops (with and
            without Shift).
        - Multi-key strategy:
          - Every 1st key can be a prefix for a family of related commands.
          - And all keys (1st and 2nd) are potentially meaningful (letters).

  - Natural language analogy: every fluent speaker already operates a
    grammatical system orders of magnitude more complex than any application's
    command vocabulary. The cognitive machinery is there; multi-key,
    grammar-based bindings can tap into that deep human skill. Modifier-based
    bindings do not.

LoopLlama as a worked example:

  - LL's binding scheme is the concrete instance of everything argued above.
    This is where the essay's occasion and its argument converge.
  - Entity vocabulary, mostly mnemonic: v = video, c = chapter, s = section, l
    = loop, m = mark, j = jump, d = delete. A new user seeing `vd` can make a
    reasonable guess.
  - The most common operations get double-letter bindings, because those are
    the easiest to type. In LL, those operations are creating new entities
    (cc, ss, ll, mm), setting scratch bounds (`[[` and `]]`), and toggling
    looping (xx) to give a few examples.
  - One non-mnemonic exception: `s` was claimed by sections, so the scratch
    loop family uses `x` as its prefix. As long as exceptions are limited in
    number, the overall scheme remains learnable. The grammar carries the
    load; isolated exceptions don't break it.
  - Which-key as the discoverability mechanism: press any prefix and the
    available completions appear. Education at the moment of motivated need --
    the user is already reaching for the command when the hint appears. This
    is the scalable version of what the Cat tried to accomplish with printed
    key labels: contextual, just-in-time, not limited to a fixed domain.
  - The cognitive demands of learning LL's binding system are lower than the
    demands of learning LL's features -- which the user has to learn
    regardless. The bindings come nearly for free once the vocabulary is
    established.

## S7: The ecosystem problem

Old transition notes:

  - Modes plus grammar is the answer Raskin was looking for: keyboard
    efficiency for everyone, not just experts, without custom hardware
    and without a fixed-domain appliance. The approach generalizes. The
    question the conclusion takes up is why, if this is available, the
    ecosystem didn't build that way -- and what it would take for it to.

  - The argument is complete on the technical level. The remaining question is
    structural: why, if this approach is available and demonstrably superior,
    did the ecosystem not converge on it?

- The path of least resistance

  - For any developer building keyboard support, the default is to
    inherit OS modifier conventions. The platform supplies them; users
    recognize them; tooling expects them. Doing something else requires
    deliberate effort against the grain.
  - Developer culture, OS APIs, user expectations, and platform
    documentation all reinforce modifier-based schemes generationally.
    Each layer of the ecosystem reflects and reproduces the choices made
    by the layer above it.
  - This is not negligence or conspiracy. It is what happens when
    infrastructure is built one way and culture follows. The path of
    least resistance leads to menus and modifiers, and most developers
    take it.

- The developer who never decided

  - Most applications with keyboard support have a modifier-based shortcut
    list accessible from the preferences menu. The developers who built
    them did not make a considered choice to reject grammar-based bindings.
    The platform offered modifiers; the tooling supported modifiers; the
    documentation showed modifiers. They followed the path. The outcome
    was determined before they started.

- The stagnation (NOTE: consider whether/where to make this point)

  - The early period of personal computing was genuinely exploratory:
    Engelbart's NLS, the Canon Cat, Smalltalk, various hardware
    experiments. People were actively trying to think about better
    ways to control computers.
  - The Mac/Windows paradigm won commercially and the book closed --
    fast, and almost completely. The major OS makers have cranked out
    new versions for decades with minimal change to the fundamental
    input paradigm. The Cat, for all its flaws, was at least trying
    something different.
  - The paradigm is self-reinforcing: users have internalized it so
    thoroughly that departures feel broken rather than better. (Win8
    as an example -- wrong direction, but illustrates how hard the
    ecosystem resists change even when someone tries.)
  - The tragedy is not only that good systems are the hard path -- it
    is also that the window for rethinking may have closed. The
    infrastructure, culture, and user expectations now all point the
    same way.

- Vim and LoopLlama swim upstream

  - Individual counter-examples prove the approach works. Vim is the
    canonical case: decades of sustained use across millions of users,
    despite (or because of) a grammar-based binding system that the
    ecosystem offered no help in building.
  - LoopLlama is a smaller case: a single-developer tool that chose the
    harder path and got a more learnable, more memorable binding system
    as a result.
  - But both are exceptions maintained by people who chose to swim
    against the current. The ecosystem sets the median outcome, not the
    exceptions. Requiring exceptional deliberateness from every developer
    to produce adequate UX is not a solution; it is a description of
    the problem.

- The counterfactual

  - If the major OS makers had built culture and infrastructure oriented
    around structured, namespace-based binding systems -- grammar-based
    binding toolkits, which-key as a platform primitive, documentation
    standards for hierarchical schemes -- the path of least resistance
    would lead somewhere better.
  - Even developers who never think carefully about UX would produce
    better defaults by following the platform. The median outcome would
    shift without requiring every developer to be exceptional.
  - The tragedy is not that good systems are impossible or even difficult
    to design. It is that the ecosystem was built in a way that makes
    them the hard path -- and then reproduced that structure across
    decades of software.

--------

[llv2]: /loopllama/
[douglas_engelbart]: https://en.wikipedia.org/wiki/Douglas_Engelbart
[nls]: https://en.wikipedia.org/wiki/NLS_(computer_system)
[mother_demos]: https://en.wikipedia.org/wiki/The_Mother_of_All_Demos
[jef_raskin]: https://en.wikipedia.org/wiki/Jef_Raskin
[humane_interface]: https://en.wikipedia.org/wiki/The_Humane_Interface
[chorded_keyboard]:  https://en.wikipedia.org/wiki/Chorded_keyboard
[fitts_law]: https://en.wikipedia.org/wiki/Fitts%27s_law
[keystroke_model]: https://en.wikipedia.org/wiki/Keystroke-level_model
[stephenson_essay]: https://web.stanford.edu/class/cs81n/command.txt
[humane_interface]: https://raskincenter.org/jef/humane-interface/
[cat_promo]: https://www.youtube.com/watch?v=o_TlE_U_X3c

[^1]: NLS include both a regular keyboard and a [chorded
    keyboard][chorded_keyboard] — a five-key device held in the non-dominant
    hand — as a dedicated command interface to use while the mouse hand was
    occupied.

[^2]: The regular characters available for key bindings consist of 26 letters,
    10 numbers, and 11 punctuation, along with space, tab, and return. Other
    keys exist and can also be used for bindings (function keys, arrows, and
    various editing or navigational keys), but those vary more across hardware
    models.

[^3]: In theory, each new modifier can double the available binding real
    estate. In practice, it did not go that far, due to various limits imposed
    by the interactions of keyboard hardware, the serial line they used, the
    ASCII standard, and other factors. For example, many `CTRL-SHIFT` bindings
    are not, at the hardware and bit levels, meaningfully distinguished from
    their `CTRL` counterparts.

